# ESA Scout - Python agent dependencies
#
# Parsing is done with Scrapling's `Selector` (adaptive, self-healing selectors).
# Fetching uses httpx by default: Scrapling's browser-backed fetchers pull in
# playwright/curl_cffi, which are heavy and flaky in CI. Install the optional
# extras below and set SCRAPLING_FETCHER=1 to route fetches through Scrapling.

# --- Scraping & parsing ---
scrapling>=0.4,<0.5
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.2

# --- Profile parsing ---
pypdf>=5.0
pdfplumber>=0.11

# --- AI evaluation (OpenAI-compatible: xAI, OpenRouter, Groq, OpenAI, ...) ---
openai>=1.40

# --- Runtime ---
python-dotenv>=1.0

# --- Optional: browser-backed Scrapling fetchers (SCRAPLING_FETCHER=1) ---
# curl_cffi>=0.7
# playwright>=1.45
